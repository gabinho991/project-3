import * as React from "react";

export function Favorites(props) {
    const socket = props.socket;
    const mealFavorites = props.mealFavorites;
    console.log(mealFavorites);
  return <div></div>;
}
